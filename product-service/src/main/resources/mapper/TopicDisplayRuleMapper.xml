<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wow.product.mapper.TopicDisplayRuleMapper">
    <resultMap id="BaseResultMap" type="com.wow.product.model.TopicDisplayRule">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="page_type" property="pageType" jdbcType="TINYINT"/>
        <result column="topic_id" property="topicId" jdbcType="INTEGER"/>
        <result column="sort_id" property="sortId" jdbcType="TINYINT"/>
        <result column="is_deleted" property="isDeleted" jdbcType="BIT"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, page_type, topic_id, sort_id, is_deleted
    </sql>
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select
        <include refid="Base_Column_List"/>
        from topic_display_rule
        where id = #{id,jdbcType=INTEGER}
    </select>
    <insert id="insert" parameterType="com.wow.product.model.TopicDisplayRule">
        <selectKey resultType="java.lang.Integer" keyProperty="id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into topic_display_rule (page_type, topic_id, sort_id,
        is_deleted)
        values (#{pageType,jdbcType=TINYINT}, #{topicId,jdbcType=INTEGER}, #{sortId,jdbcType=TINYINT},
        #{isDeleted,jdbcType=BIT})
    </insert>
    <insert id="insertSelective" parameterType="com.wow.product.model.TopicDisplayRule">
        <selectKey resultType="java.lang.Integer" keyProperty="id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into topic_display_rule
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="pageType != null">
                page_type,
            </if>
            <if test="topicId != null">
                topic_id,
            </if>
            <if test="sortId != null">
                sort_id,
            </if>
            <if test="isDeleted != null">
                is_deleted,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="pageType != null">
                #{pageType,jdbcType=TINYINT},
            </if>
            <if test="topicId != null">
                #{topicId,jdbcType=INTEGER},
            </if>
            <if test="sortId != null">
                #{sortId,jdbcType=TINYINT},
            </if>
            <if test="isDeleted != null">
                #{isDeleted,jdbcType=BIT},
            </if>
        </trim>
    </insert>
</mapper>