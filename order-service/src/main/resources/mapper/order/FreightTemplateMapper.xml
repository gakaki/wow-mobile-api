<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wow.order.mapper.FreightTemplateMapper">
    <resultMap id="BaseResultMap" type="com.wow.order.model.FreightTemplate">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="charging_rul" property="chargingRul" jdbcType="TINYINT"/>
        <result column="is_shipping_free" property="isShippingFree" jdbcType="BIT"/>
        <result column="shipping_free_min_amount" property="shippingFreeMinAmount" jdbcType="SMALLINT"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, charging_rul, is_shipping_free, shipping_free_min_amount
    </sql>
    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select
        <include refid="Base_Column_List"/>
        from freight_template
        where id = #{id,jdbcType=INTEGER}
    </select>
    <insert id="insert" parameterType="com.wow.order.model.FreightTemplate">
        <selectKey resultType="java.lang.Integer" keyProperty="id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into freight_template (charging_rul, is_shipping_free, shipping_free_min_amount
        )
        values (#{chargingRul,jdbcType=TINYINT}, #{isShippingFree,jdbcType=BIT},
        #{shippingFreeMinAmount,jdbcType=SMALLINT}
        )
    </insert>
    <insert id="insertSelective" parameterType="com.wow.order.model.FreightTemplate">
        <selectKey resultType="java.lang.Integer" keyProperty="id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into freight_template
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="chargingRul != null">
                charging_rul,
            </if>
            <if test="isShippingFree != null">
                is_shipping_free,
            </if>
            <if test="shippingFreeMinAmount != null">
                shipping_free_min_amount,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="chargingRul != null">
                #{chargingRul,jdbcType=TINYINT},
            </if>
            <if test="isShippingFree != null">
                #{isShippingFree,jdbcType=BIT},
            </if>
            <if test="shippingFreeMinAmount != null">
                #{shippingFreeMinAmount,jdbcType=SMALLINT},
            </if>
        </trim>
    </insert>
</mapper>